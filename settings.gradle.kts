// List every provider module here. Add a new line each time you add a new provider folder.
include(":CineHub24")

// If you add more providers later, e.g. a folder called "MyOtherSite", add:
// include(":MyOtherSite")
